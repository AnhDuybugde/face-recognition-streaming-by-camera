services:
  face-recognition:
    build:
      context: .
    image: streaming-face-recognition:cpu
    volumes:
      - ./models:/app/models:ro
      - ./data:/app/data:ro
    command: >-
      python -m scripts.run_image_recognition
      --input /app/data/test.jpg
      --iterations 10
